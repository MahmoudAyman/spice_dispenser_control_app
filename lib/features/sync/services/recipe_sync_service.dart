
import '../../recipes/data/models/recipe_model.dart';

class RecipeSyncService {

  Future<void> syncRecipeToMachine(
      RecipeModel recipe,
      ) async {

    /// send recipe packet to ESP32

    /// wait ack

    /// save locally
  }
}